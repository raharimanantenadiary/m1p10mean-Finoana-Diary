export class Fiara{
    make: string;
    model: string;
    year: number;
    id: number;
  
    constructor(make: string, model: string, year: number, id: number) {
      this.make = make;
      this.model = model;
      this.year = year;
      this.id = id;
    }
}